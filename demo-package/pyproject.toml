[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "smart-manufacturing-demo"
version = "1.0.0"
description = "命题188智能制造比赛演示包"
requires-python = ">=3.8"
dependencies = ["openpyxl>=3.1,<4"]

[project.scripts]
smart-manufacturing-demo = "main:main"
